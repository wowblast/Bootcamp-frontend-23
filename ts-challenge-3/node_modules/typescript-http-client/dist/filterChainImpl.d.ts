import { FilterChain, InstalledFilter, Request, Response } from './index';
export default class FilterChainImpl implements FilterChain<unknown> {
    readonly filters: InstalledFilter[];
    readonly fromIndex: number;
    readonly callBack: (request: Request) => Promise<Response<unknown>>;
    constructor(filters: InstalledFilter[], fromIndex?: number, callBack?: (request: Request) => Promise<Response<unknown>>);
    doFilter(request: Request): Promise<Response<unknown>>;
}
